declare class WebSocketDriver implements IDriver {
    ws: WebSocket;
    connect(done: any): void;
    setMessageHandler(onMessage: (message: string) => any): any;
    send(message: any): void;
}
export = WebSocketDriver;
