import { Observable } from "rxjs/Observable";
declare class ServiceConnection {
    private driver;
    constructor(driver: IDriver, onReady: (connection: ServiceConnection) => any);
    sendRequest(request: ts.server.protocol.Request): void;
    subscribe(request: ts.server.protocol.Request): Observable<ts.server.protocol.Diagnostic[]>;
    sendRequestResp<Resp>(request: ts.server.protocol.Request): Observable<Resp>;
    private reqResponseSubjects;
    private semanticEventsSuject;
}
export = ServiceConnection;
